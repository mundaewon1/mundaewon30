<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moit.reports.dao.ReportsMapper">


	<!-- ===== user ===== -->
	<!-- ===== user ===== -->
	<!-- 신고 작성  /     서비스에서  - target_type  (MEETUP , REVIEW )   -->
	<insert id="insertUserReport" parameterType="ReportsDto">
		insert into reports (report_id, target_type, target_id, member_id, reason_code, reason_detail)
		values ( reports_seq.nextval, #{targetType}, #{targetId}, #{memberId}, #{reasonCode}, #{reasonDetail} )
	</insert>
  
	<!-- 사용자 본인이 작성한 신고 내역 수정 -->
	<update id="updateUserReport" parameterType="ReportsDto">
		update reports
		set reason_code = #{reasonCode}, reason_detail = #{reasonDetail}, updated_at = sysdate
		where report_id = #{reportId} and member_id = #{memberId} and delete_yn = 'N' and status = 'PENDING'
	</update>

	<!-- 사용자 본인이 작성한 신고 내역 삭제 (update delete_yn = 'Y') -->
	<update id="deleteUserReport" parameterType="ReportsDto">
		update reports
		set delete_yn = 'Y'
		where report_id = #{reportId} and member_id = #{memberId} and delete_yn = 'N' and status = 'PENDING'
	</update>

	<!-- 사용자 본인이 작성한 신고 내역 조회 & 유저 - 페이징 -->
	<select id="selectUserReport" resultType="ReportsDto"   parameterType="java.util.HashMap">
		select report_id, target_type, target_id, member_id, reason_code, reason_detail, status, created_at
		from reports
		where member_id = #{memberId} and delete_yn = 'N'
		order by report_id desc
		offset #{start} rows fetch next #{end} rows only
	</select>
	
	<select id="selectUserCnt" resultType="int">
		select count(*) from reports 
		where member_id = #{memberId} and delete_yn = 'N'
	</select>

	<!-- 사용자 본인이 작성한 신고 내역 상세 조회 -->
	<select id="selectUserReportDetail" resultType="ReportsDto" parameterType="ReportsDto">
		select report_id, target_type, target_id, member_id, reason_code, reason_detail, status, created_at, updated_at
		from reports
		where report_id = #{reportId} and member_id = #{memberId} and delete_yn = 'N'
	</select>
	
	<!-- 중복 신고 방지 -->
	<select id="doubleReport" resultType="int" parameterType="ReportsDto">
		select count(*)
		from reports
		where member_id = #{memsberId}
			and target_type = #{targetType}
			and target_id = #{targetId}
			and delete_yn = 'N'
	</select>
	
	<!-- 신고 난사 방지 -->
	<select id="TodayReport" resultType="int" parameterType="ReportsDto">
		select count(*)
		from reports
		where member_id = #{memberId} and delete_yn = 'N'
			and created_at >= trunc(sysdate)
			and created_at &lt; trunc(sysdate) +1
	</select>
	
	

	
	<!-- ===== admin ===== -->
	<!-- ===== admin ===== -->
	<!-- 신고당한 글 작성자 select (targetMemberId) -->
	<!-- meetup / review , where = report_id -->
	<select id="selectTargetMemberId" resultType="int" parameterType="ReportsDto">
		select target_member_id
		from (
			select	r.report_id,
					mt.member_id as target_member_id
			from reports r		join meetups mt		on r.target_id = mt.meetup_id
			where r.target_type = 'MEETUP'
			
			union all
			
			select	r.report_id,
					rv.member_id as target_member_id
			from reports r		join reviews rv		on r.target_id = rv.review_id
			where r.target_type = 'REVIEW'
		)
		where report_id = #{reportId}
	</select>
	
	<!-- 신고당한 글 작성자 select (targetMemberId + members.nickname) -->
	<select id="selectTargetMemberNickname" resultType="String" parameterType="ReportsDto">
		select target_nickname
		from (
			select	r.report_id,
					m.nickname as target_nickname
			from reports r
			join meetups mt		on r.target_id = mt.meetup_id
			join members m		on mt.member_id = m.member_id
			where r.target_type = 'MEETUP'
			
			union all
			
			select	r.report_id,
					m.nickname as target_nickname
			from reports r
			join reviews rv		on r.target_id = rv.review_id
			join members m		on rv.member_id = m.member_id
			where r.target_type = 'REVIEW'
		)
		where report_id = #{reportId}
	</select>
	
	<!-- APPROVED 승인 건수 조회 (MEETUP + REVIEW) -->
	<select id="approvedCnt" resultType="int" parameterType="ReportsDto">
		select count(*)
		from (
			select mt.member_id as target_member_id
			from reports r		join meetups mt		on r.target_id = mt.meetup_id
			where r.target_type = 'MEETUP' and r.status = 'APPROVED'
				
			union all
				
			select rv.member_id as target_member_id
			from reports r		join reviews rv		on r.target_id = rv.review_id
			where r.target_type = 'REVIEW' and r.status = 'APPROVED'
		)		
		where target_member_id = #{targetMemberId}
	</select> 
	
	
	
	<!-- 신뢰도 점수 select 
	<select id="selectTrustScore" resultType="int" parameterType="ReportsDto">
		select member_info
		set trust_score
		where report_id = #{targetMemberId}
	</select> -->
	
	<!-- 뱃지 select
		member_info.report_status_id를 기준으로 꺼내고,
		실제 표시명은 member_report_status랑 JOIN해서 가져오는 방향
	<select id="selectBadge" resultType="String" parameterType="ReportsDto">
		select report_status_id
		from member_info mi		join member_report_status mrs
								on mi.report_status_id = mrs.report_status_id
		where mi.member_id = mrs.report_status_id
	</select> -->
	
	<!-- 신뢰도 점수 update (member_info.trust_score) 
	<update id="updateTrustScore" parameterType="ReportsDto">
		update member_info
		set trust_score = (
			select 100 - (count(*) * 10)
			from (
				select mt.member_id as target_member_id
				from reports r		join meetups mt		on r.target_id = mt.meetup_id
				where r.target_type = 'MEETUP' and r.status = 'APPROVED'
	
				union all
				
				select rv.member_id as target_member_id
				from reports r		join reviews rv		on r.target_id = rv.review_id
				where r.target_type = 'REVIEW' and r.status = 'APPROVED'
			)
			where target_member_id = #{targetMemberId}
		)
		where member_id = #{targetMemberId}
	</update> -->
	
	<!-- 뱃지 1/2/3 update (member_info.report_status_id) 
	<update id="updateBadge" parameterType="ReportsDto">
		update member_info
		set report_status_id = #{reportStatusId}
		where member_id = #{targetMemberId}
	</update> -->
	
	
	
	
	
	<!-- 실시간 신뢰점수 산출 쿼리 (새벽 배치 타겟 정산용 - 인덱스 최적화 완비)
		기본 점수 100점 + (승인 횟수 * 2) - (노쇼 * 10) - (신고당한 건수 * 5)
	        (mi.trust_score
			
			approved_cnt = 모임 참여 승인 횟수		→ meetup_applications.status = 'APPROVED'
			noshow_cnt = 모임 노쇼 횟수 			→ meetup_applications.status = 'NOSHOW'
			report_cnt = 내가 작성한 '모임(MEETUP)'이 신고 승인(APPROVED)된 횟수 추출
			내가 작성한 '후기(REVIEW)'가 신고 승인(APPROVED)된 횟수 추출 -->
			
	<select id="calTrustScore" resultType="int">
	    select 
	        greatest( 0,
			    least( 100,
			        100
			        + coalesce(ma.approved_cnt, 0) * 2
			        - coalesce(ma.noshow_cnt, 0) * 10
			        - ( coalesce(r_met.report_cnt, 0) + coalesce(r_rev.report_cnt, 0) ) * 5
			    )
			) as calculated_trust_score
	    from members m
	    join member_info mi on m.member_id = mi.member_id
	
		    left join (
		        select 
		            member_id,
		            count(case when status = 'APPROVED' then 1 end) as approved_cnt,
		            count(case when status = 'NOSHOW' then 1 end) as noshow_cnt
		        from meetup_applications
		        where member_id = #{memberId}
		          and updated_at >= sysdate - 90   
		        group by member_id
		    ) ma
		    on m.member_id = ma.member_id
		
		    
		    left join (
		        select 
		            mt.member_id,
		            count(r.report_id) as report_cnt
		        from reports r
		        join meetups mt
		            on r.target_type = 'MEETUP' and r.target_id = mt.meetup_id
		        where mt.member_id = #{memberId}
		          and r.status = 'APPROVED'
		          and r.delete_yn = 'N'
		          and r.updated_at >= sysdate - 90 
		        group by mt.member_id
		    ) r_met
		    on m.member_id = r_met.member_id
		
		    left join (
		        select 
		            rv.member_id,
		            count(r.report_id) as report_cnt
		        from reports r
		        join reviews rv on r.target_type = 'REVIEW' and r.target_id = rv.review_id
		        where rv.member_id = #{memberId}
		          and r.status = 'APPROVED'
		          and r.delete_yn = 'N'
		          and r.updated_at >= sysdate - 90 
		        group by rv.member_id
		    ) r_rev
		    on m.member_id = r_rev.member_id
	
	    where m.member_id = #{memberId}
	</select> 

	
	<!-- 단순 조회용 쿼리 (로그인/마이페이지 호출용 member_info.trust_score - DB 부하 0%) -->
	<!-- 신고자, 닉네임, 신뢰도 점수, 신고 상태(report_status_id), 신고 상태(code), 신고 상태(name) -->
	<select id="findMemberTrustInfo" resultType="ReportsDto" parameterType="ReportsDto">
		select	m.member_id				as targetMemberId,	<!-- 신고당한 유저 -->
				m.nickname				as targetNickname,	<!-- 신고당한 유저 닉네임 -->
				mi.trust_score			as trustScore,		<!-- 신뢰도 점수 -->
				mi.report_status_id		as reportStatusId,	<!-- 뱃지 num -->
				mrs.status_code			as statusCode,		<!-- 뱃지 (eng) -->
				mrs.status_name			as statusName		<!-- 뱃지 (kor) -->
		from members m
			left join member_info mi			on m.member_id = mi.member_id
			left join member_report_status mrs	on mi.report_status_id = mrs.report_status_id
		where m.member_id = #{targetMemberId}
	</select>
	
	<!-- 실시간 신뢰점수 산출 count(*) 쿼리 3개
		기본 점수 100점 + (승인 횟수 * 2) - (노쇼 * 10) - (신고당한 건수 * 5) -->
	<!-- approved_cnt = 모임 참여 승인 횟수 (+2) meetup_applications.status = 'APPROVED'  -->
	<select id="selectApprovedCnt" resultType="int" parameterType="int">
		select count(*)
		from meetup_applications
		where member_id = #{memberId}
			and status = 'APPROVED'
			and updated_at >= sysdate - 90
	</select> 
	
	<!-- noshow_cnt = 모임 노쇼 횟수 (-10) meetup_applications.status = 'NOSHOW' -->
	<select id="selectNoshowCnt" resultType="int" parameterType="int">
		select count(*)
		from meetup_applications
		where member_id = #{memberId}
			and status = 'NOSHOW'
			and updated_at >= sysdate - 90
	</select> 
	
	<!-- report_cnt = 내가 작성한 MEETUP/REVIEW가 신고 승인된 횟수 (-5) -->
	<select id="selectReportCnt" resultType="int" parameterType="int">
		select count(*)
		from (
			select r.report_id,
					mt.member_id as target_member_id
			from reports r	join meetups mt
				on r.target_type = 'MEETUP'
					and r.target_id = mt.meetup_id
					and r.status = 'APPROVED'
					and r.delete_yn = 'N'
					and r.updated_at >= sysdate -90
					
			union all
			
			select r.report_id,
					rv.member_id as target_member_id
			from reports r	join reviews rv
				on r.target_type = 'REVIEW'
					and r.target_id = rv.review_id
					and r.status = 'APPROVED'
					and r.delete_yn = 'N'
					and r.updated_at >= sysdate -90
		)
		where target_member_id = #{targetMemberId}
	</select> 
	
	
	
	
	
	<!-- 계산된 신뢰도 점수 업데이트 쿼리 -->
	<update id="updateMemberTrustScore" parameterType="ReportsDto">
		update member_info
		set trust_score = #{trustScore}
		where member_id = #{memberId}
	</update>
	<!-- 뱃지 업데이트 쿼리 -->
	<update id="updateMemberBadge" parameterType="ReportsDto">
		update member_info
		set report_status_id = #{reportStatusId}
		where member_id = #{memberId}
	</update>
	
	<!-- 새벽 배치용: 전날 활동 이력(참여APPROVED/노쇼NOSHOW/신고(reports.status = 'APPROVED' 처리 이력)이 있는 회원 ID 타겟 추출 -->
	<!-- 모임 신청 상태가 'APPROVED' OR 'NOSHOW'double 회원 member_id -->
	<!-- MEETUP 에서 신고 처리가 'APPROVED' 된 모임글 작성자 mt.member_id -->
	<!-- REVIEW 에서 신고 처리가 'APPROVED' 된 후기글 작성자 rv.member_id -->
	<select id="selectTargetMembersYesterday" resultType="ReportsDto">
	    select member_id
	    from meetup_applications
	    where status in ('APPROVED', 'NOSHOW')
	        and (updated_at >= trunc(sysdate) -1 and updated_at &lt; trunc(sysdate))
	            
	    union
	    
	    select mt.member_id
	    from reports r
	    join meetups mt on r.target_type = 'MEETUP'
	    					and r.target_id = mt.meetup_id
	    where r.status = 'APPROVED'
	        and (r.updated_at >= trunc(sysdate) -1 and r.updated_at &lt; trunc(sysdate))
	
	    union
	    
	    select rv.member_id
	    from reports r
	    join reviews rv on r.target_type = 'REVIEW'
	    					and r.target_id = rv.review_id
	    where r.status = 'APPROVED'
	        and (r.updated_at >= trunc(sysdate) -1 and r.updated_at &lt; trunc(sysdate))
	</select>
	
	
	
	<!-- 3일 전에 신고 상태 변경된 데이터 추출 -->
	<select id="selectThreeDaysAgo" resultType="ReportsDto">
		select m.email
		from reports r join members m on r.member_id = m.member_id
		where r.status = 'APPROVED'
			and r.updated_at >= trunc(sysdate) - 3
			and r.updated_at &lt; trunc(sysdate) - 2
	</select> 
	
	<!-- 당일에 신고 상태 변경된 데이터 추출 
	<select id="selectThreeDaysAgo" resultType="ReportsDto">
	    select m.email
	    from reports r join members m on r.member_id = m.member_id
	    where r.status = 'APPROVED'
	    	and r.updated_at >= trunc(sysdate) 
	</select> -->
	
	
	
	
	
	<!-- 신고 상태 변경 - PENDING(처리대기) or APPROVED(신고완료) -->
	<update id="updateAdmin" parameterType="ReportsDto">
	    update reports
	    set status = #{status}
	    where report_id = #{reportId}
	    and status = 'PENDING'
	</update>
	<!-- members table에 report_id로 email 찾기 -->
	<select id="selectEmail" resultType="String" parameterType="ReportsDto">
		select m.email
		from reports r join members m on r.member_id = m.member_id
		where r.report_id = #{reportId}
	</select>
	

	<!-- 신고 강제 삭제 -->
	<delete id="deleteAdmin" parameterType="int">
		delete
		from reports
		where report_id = #{reportId}
	</delete>
	
	<!-- 전체 신고 대상 목록 조회 & 관리자 - 페이징 -->
	<!-- 신고 대상 유형별 목록 조회 - MEETUP & REVIEW (targetType) -->
	<!-- 신고 대상 유형별 목록 조회 - PENDING (status) -->
	<!-- 신고 검색(작성자) - 관리자 -->
	<!-- 신고 검색(사유) - 관리자 -->
	<!-- 신고 검색(날짜) - 관리자 -->
	<!-- 관리자 신고 목록 조회 (동적 조건 + 페이징) -->
	<!-- 신고 목록 상세 조회 --> 
	<select id="selectAdminReports" resultType="ReportsDto" parameterType="java.util.HashMap">
	    select *
	    from reports
	    <where>
	    	<if test="deleteYn != null and deleteYn != ''">
	    		and delete_yn = #{deleteYn}
	    	</if>
	        <if test="reportId != null">
	            and report_id = #{reportId}
	        </if>
	        <if test="status != null and status != ''">
	            and status = #{status}
	        </if>
	        <if test="targetType != null and targetType != ''">
	            and target_type = #{targetType}
	        </if>
	        
	        <if test="keyword != null and keyword != ''">
		        <choose>
		        	<!-- 작성자 ID 검색 -->
			        <when test="searchType == 'memberId'">
			            and to_char(member_id) = #{keyword}
			        </when>
			        <!-- 신고 사유 검색: 대소문자 구분하지 않음 -->
			        <when test="searchType == 'reasonCode'">
	                    and upper(reason_code) like '%' || upper(#{keyword}) || '%'
	                </when>
			        <!-- 날짜 검색: 2026-07-26 형식 -->
	                <when test="searchType == 'createdAt'">
	                    and trunc(created_at) = to_date(#{keyword}, 'YYYY-MM-DD')
	                </when>
		        </choose>
	        </if>
	    </where>
	    order by report_id desc
	    <if test="start != null and end != null">
	        offset #{start} rows fetch next #{end} rows only
	    </if>
	</select>

	<!-- 관리자 신고 목록 카운트 (동적 조건 반영) -->
	<select id="selectAdminReportsCnt" resultType="int" parameterType="java.util.HashMap">
	    select count(*)
	    from reports
	    
	    <where>
		    <if test="deleteYn != null and deleteYn != ''">
		    	and delete_yn = #{deleteYn}
		    </if>
	        <if test="status != null and status != ''">
	            and status = #{status}
	        </if>
	        <if test="targetType != null and targetType != ''">
	            and target_type = #{targetType}
	        </if>
	        <if test="keyword != null and keyword != ''">
	            <choose>
	                <when test="searchType == 'memberId'">
	                    and to_char(member_id) = #{keyword}
	                </when>
	
	                <when test="searchType == 'reasonCode'">
	                    and upper(reason_code) like '%' || upper(#{keyword}) || '%'
	                </when>
	
	                <when test="searchType == 'createdAt'">
	                    and trunc(created_at) = to_date(#{keyword}, 'YYYY-MM-DD')
	                </when>
	            </choose>
	        </if>
	        
	        <!-- 
	        <if test="memberId != null">
	            and member_id = #{memberId}
	        </if>
	        <if test="reasonCode != null and reasonCode != ''">
	            and reason_code = #{reasonCode}
	        </if>
	        <if test="createdAt != null and createdAt != ''">
	            and date(created_at) = #{createdAt}
	        </if>  -->
	        
	    </where>
	</select>

	
 

	
</mapper>



	
	
	<!-- 유저 - 페이징 (사용자 본인이 작성한 신고 내역 조회 selectUserReport) -->
	<!-- 관리자 - 페이징 (전체 신고 대상 목록 조회 selectAdminReport) -->
	
	
	<!-- 사유별 신고 건수 집계 
	<select id="selectReasonReportCount" resultType="ReportsDto">
		select reason_code, count(*) as reportCount
		from reports
		group by reason_code;
	</select> -->
	
	<!-- 회원별 누적 신고 횟수 
	<select id="selectMemberReportCount" resultType="ReportsDto">
		select member_id, count(*) as reportCount
		from reports
		group by member_id;
	</select> -->
		
	<!-- 대상별 신고 현황 (모임/후기) 
	<select id="selectTargetReportCount" resultType="ReportsDto">
		select target_type, target_id, count(*) as reportCounts
		from reports
		group by target_type, target_id;
	</select> -->
	
	 